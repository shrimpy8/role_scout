"""role_scout.compat — verbatim transplant of auto_jobsearch (Phase 1) code.

All jobsearch.* imports have been rewritten to role_scout.compat.* so this
package is self-contained. Never modify these files to add Phase 2 logic;
keep them as a faithful copy of the Phase 1 contract.
"""
